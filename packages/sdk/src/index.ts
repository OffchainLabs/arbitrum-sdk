/**
 * @arbitrum/sdk — backwards-compatible re-export from @arbitrum/ethers5.
 *
 * This is a major version bump. The old class-based API
 * (EthBridger, Erc20Bridger, ParentTransactionReceipt, etc.)
 * is replaced by a functional API. See migration guide.
 *
 * All exports now come from @arbitrum/ethers5.
 */
export * from '@arbitrum/ethers5'
