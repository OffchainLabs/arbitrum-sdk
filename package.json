{
  "name": "@arbitrum/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/typechain/**",
      "**/@typechain/**"
    ]
  },
  "resolutions": {
    "@ethersproject/*": "^5.1.0"
  },
  "scripts": {
    "build": "yarn workspace @arbitrum/sdk build",
    "test": "yarn workspace @arbitrum/sdk test",
    "lint": "yarn workspace @arbitrum/sdk lint",
    "format": "yarn workspace @arbitrum/sdk format"
  }
}
